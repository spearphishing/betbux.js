export type LoginFieldData = {
	message: string;
	userFacingMessage: string;
	fieldData: string;
	userLocation: string;
};

export type SuccessfulLoginData = {
	betBuxCookie: string;
	userLocation: string;
};
